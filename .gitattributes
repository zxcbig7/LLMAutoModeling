# 換行字元(line ending)由 git 釘死,避免跨機器 / Download ZIP 後排版跑掉。
#
# 政策:repo 一律存 LF;所有文字檔在 checkout 與 git archive(= GitHub
# Download ZIP)時「強制 CRLF」。用明確 eol=crlf 而非 text=auto,
# 才能保證連 GitHub 在 Linux 產生的 ZIP 也是 CRLF(Windows 工具才不會擠成一行)。
#
# 例外:.sh 必須 LF;二進位完全不轉換。
* text=auto eol=crlf

# ── 必須是 LF(否則在 Linux/容器執行會壞)──
*.sh   text eol=lf

# ── 二進位:完全不做換行轉換(明確標記以保護)──
*.dll   binary
*.exe   binary
*.pdb   binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.zip   binary
*.gz    binary
*.7z    binary
*.pdf   binary
*.xlsx  binary
*.xls   binary
*.docx  binary
*.pptx  binary
